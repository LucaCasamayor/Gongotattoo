spring.application.name=gongotattoo

# MySQL database
spring.datasource.url=jdbc:mysql://localhost:3306/gongotattoo?useSSL=false&serverTimezone=UTC
spring.datasource.username=gongouser
spring.datasource.password=gongopass
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# JWT config
app.jwt.secret=${JWT_SECRET:super-secreto-cambiar}
app.jwt.expiration-ms=3600000


